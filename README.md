# CEVI
CEVI — Android-on-Linux toolbox: connect your phone over Wi-Fi (ADB), mirror &amp; record its screen with scrcpy + v4l2loopback, run diagnostics and install dependencies with an explicit sudo password prompt. Plugin-based: drop a Python file in actions/ and it works in both CLI and PyQt6 GUI.
